describe("The payment manager card", () => {
  beforeEach(() => {
    cy.visit("/payments");
  });
  it("should display both net and gross values if api response has net value", () => {});
});

describe("The request payment modal", () => {
  beforeEach(() => {
    cy.visit("/payments");
  });
  it("should display both net and gross values if api response has net value", () => {});
});
